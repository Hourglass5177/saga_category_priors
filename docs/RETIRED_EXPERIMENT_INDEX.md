# 已退出主线的实验索引

更新日期：2026 年 9 月 3 日

这是一张导航表，不是旧代码的兼容层。当前运行标准只认
[`category_priors/INSTANCE_RECHECK_BASELINE_STANDARD.md`](../category_priors/INSTANCE_RECHECK_BASELINE_STANDARD.md)。
旧实现继续保存在 Git 历史和只读实验产物中，不应重新接入当前主线。

| 路线 | 代表性末端提交 | 仍可保留的结论 | 为什么退出主线 / 产物位置 |
| --- | --- | --- | --- |
| B0 / B1 老师兼容流程 | `3c07620`、汇总见 `74a745b` | B1 小物体分支能救回少量实例，也会增加假实例；公开 SAGA 不是这条自动流程的数值基线 | 后续实验仍以修正 contributor 和输出合同后的 B0/B1 作历史锚点；旧云端 `teacher-baseline-closure` 产物只读 |
| B2 overlay | `f1367fa` | 分支实例会在统一过滤和全场近邻中被吞掉，属于真实接口现象 | 保护式恢复改变了后处理结构，不能单独证明类别统计有效 |
| class-first | `ce090d7` | 过早按语义类别切分会把类别错误传播到实例形成 | 候选主干和类别先验同时变化，因果不可分 |
| prior-v2 | `c66b0de` | 训练集类别统计可计算，但在不健康候选上无法解释收益 | 候选形成、打分和恢复接口混杂 |
| teacher-prior V3–V6 | `e96d7ba` | 候选供给和候选健康度是先验验证前置条件 | 多轮保护、融合和候选重建偏离老师要求的最小检验 |
| V7–V10 ObjectBank / 多视角对象跟踪 | `00baee6` | 精确逐帧掩码成员关系没有作为旧后处理的运行时对象图持续保留 | 新系统自身的关联与重建质量主导结果，已变成另一条自动实例主干 |
| 提示式尺度门控与 10k 容量正控 | `0528f95` | 现有尺度响应不够强、稳定，扩大训练没有形成有力正证据 | 类别先验被绑定到特征尺度控制，不是老师最新提出的二维复核接口 |
| 全类别降噪与候选形成 / HDBSCAN 修复 | `ccb5677` | 中心重分配、候选扩张和后处理会改变小簇；原始候选中真候选很少 | 继续换聚类或修候选会把问题扩成主干重构 |
| clean alpha-mask baseline 与 late-filter | `64505e0` | 掩码支持、歧义和检测比例过滤都会造成阶段损失 | 路线逐渐取代老师基线，不能直接回答类别裁图是否有用 |
| KNN 前完整实例三维尺寸排序 | `b41533e` | 能更干净地看尺寸排序，但仍停留在三维打分和恢复 | 老师最新建议要求把候选回到二维图像重新确认 |

三个历史锚点：

- `74a745b`：大清理前的完整研究审计与历史汇总；
- `b41533e`：大清理前最后一条完整实例尺寸闭环代码；
- `e6a9bfe`：删除旧运行主干、转入二维复核前的基线重置提交。

本机未跟踪旧产物的可恢复备份位于
`F:\3DGS_Research\saga-retired-local-20260903`。其他结果仍可能位于历史云电脑的
`/root/autodl-tmp/saga/artifacts/` 和 `/root/autodl-tmp/saga/runs/`；没有完成身份核验的文件只能作法证材料，不能直接作为当前实验输入。
